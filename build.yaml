name: test container

on:
  push:

jobs:
  container-job:
    runs-on: ubuntu-latest
    container: maven:3.8.5-amazoncorretto-11
    
    steps:
      - name: Test
        run: |
          mvn clean install
